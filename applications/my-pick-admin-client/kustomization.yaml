apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- configmap.yaml

# Secret은 템플릿만 제공, 현재는 사용하지 않음
# - secret.yaml

commonLabels:
  app: my-pick-admin-client
  managed-by: kustomize
