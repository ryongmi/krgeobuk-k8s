# mypick-admin-client Service
# MyPick Admin Client service configuration for Kubernetes

apiVersion: v1
kind: Service
metadata:
  name: mypick-admin-client
  labels:
    app: mypick-admin-client
    tier: frontend
    component: mypick-admin
spec:
  type: ClusterIP
  selector:
    app: mypick-admin-client
  ports:
  # Next.js HTTP port
  - name: http
    port: 80
    targetPort: 3310
    protocol: TCP

  # Session affinity for better performance
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800  # 3 hours
