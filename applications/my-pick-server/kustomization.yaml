apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- configmap.yaml

# Secret은 템플릿만 제공, 실제 secret.yaml은 수동으로 생성
# - secret.yaml

commonLabels:
  app: my-pick-server
  managed-by: kustomize
